# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

testsprite_tests/tmp/config.json
.node/

# Environment / secrets — never commit these
.env.local
.env.*.local
backend/.env.local

# Uploaded user files (stored on disk, not in git)
backend/uploads/

# SQLite database (local dev only)
backend/prisma/*.db
backend/prisma/*.db-journal
